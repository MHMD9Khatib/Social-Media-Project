const myError = require('./costm-error');
const notFoundError = require('./not-found');
const serverError = require('./server-error');

module.exports = { 
    myError, 
    notFoundError, 
    serverError 
};

